{
  "//": "Copy the dynamic-ui block into your Claude Desktop config and restart Claude Desktop.",
  "//config_path_windows": "%APPDATA%\\Claude\\claude_desktop_config.json",
  "//config_path_macos": "~/Library/Application Support/Claude/claude_desktop_config.json",
  "//note": "The server is one shared HTTP instance (start it with `dotnet run` in server/). Desktop's config talks stdio, so mcp-remote bridges stdio<->the HTTP MCP endpoint. --allow-http is required because it's plain http on localhost (no TLS). Needs Node/npx on PATH.",
  "mcpServers": {
    "dynamic-ui": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "http://localhost:5179/mcp",
        "--allow-http"
      ]
    }
  }
}
